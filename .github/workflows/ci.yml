name: CI

on:
  push:
    branches:
      - develop

  pull_request:
    branches:
      - '*'

jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [ 'ubuntu-latest' ]
        php-versions: [ '8.3', '8.4' ]
    name: PHP ${{ matrix.php-versions }} Test on ${{ matrix.operating-system }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set Up PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}
          extensions: mbstring
          coverage: xdebug

      - name: Display PHP Version
        run: php -v

      - name: Update Composer to Latest Version
        run: composer self-update

      - name: Install Composer Dependencies
        run: composer install --optimize-autoloader

      - name: Validate Composer Configuration
        run: composer validate --strict

      - name: Check platform requirements for packages
        run: composer check

      - name: Analyze Composer Dependencies
        run: composer composer-dependency-analyser

      - name: Lint PHP Files
        run: |
          find src tests -name '*.php' -exec php -l {} \;

      - name: Run Tests and Generate Coverage Report
        run: |
          composer test -- --coverage-clover=coverage.xml
          cat coverage.xml

      - name: Check Code Style with PHP CS Fixer
        run: |
          if [[ "${{ matrix.php-versions }}" != "8.4" ]]; then
            composer cs-check
          fi

      - name: Analyze Code with PHPStan
        run: vendor/bin/phpstan analyse --level=max --error-format=table --memory-limit=1G

      - name: Perform Composer Security Audit
        run: composer audit --format=table

      - name: Normalize Composer Configuration
        run: composer normalize --dry-run --no-check-lock

      - name: Extract coverage percentage
        id: extract
        run: |
          LINE=$(grep 'Lines:' coverage.xml || echo "")
          if [ -z "$LINE" ]; then
            echo "No coverage info found"
            exit 1
          fi
          PERCENT=$(echo "$LINE" | awk '{print $3}' | tr -d '%')
          echo "percent=$PERCENT" >> $GITHUB_OUTPUT

      - name: Generate coverage badge
        run: |
          PERCENT=${{ steps.extract.outputs.percent }}
          COLOR=red
          if (( $(echo "$PERCENT >= 80" | bc -l) )); then COLOR=green;
          elif (( $(echo "$PERCENT >= 50" | bc -l) )); then COLOR=yellow;
          fi
          curl -s -o coverage.svg "https://badgen.net/badge/coverage/${PERCENT}%25/${COLOR}"

      - name: Commit and push coverage badge
        if: github.ref == 'refs/heads/develop'  # only push badge on develop
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add coverage.svg
          git commit -m "Update coverage badge to ${{ steps.extract.outputs.percent }}%" || echo "No changes"
          git push
